apply plugin: "war"

bootWar {
    mainClassName = "com.emu.rule_engine_ms.RuleEngineMsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
